// Disini adalah basic understanding dari bahasa pemrograman Go.
// Go adalah bahasa pemrograman yang sederhana, efisien, dan mudah dipelajari.

// Pembelajaran repository ini mencakup konsep dasar seperti:

1.  Basic syntax
    Tipe data

    ````func main() {
    var name string = "Rafi"
    age := 20
    height := 170.5

        		fmt.Println(name, age, height)
        	}```
    ````

Operator

```fmt.Println(a > b)
fmt.Println(a == b)
fmt.Println(a != b)

    	x := true
    	y := false

    	fmt.Println(x && y) // AND
    	fmt.Println(x || y) // OR
    	fmt.Println(!x)     // NOT

```

\*If else
```nilai := 85

    if nilai >= 90 {
    	fmt.Println("A")
    } else if nilai >= 75 {
    	fmt.Println("B")
    } else {
    	fmt.Println("C")
    }```

\*Loop
`for i := 0; i < 5; i++ {
    fmt.Println(i)
}`

\*Function
```func tambah(a int, b int) int {
return a + b
}

    func main() {
    	hasil := tambah(5, 3)
    	fmt.Println(hasil)
    }```

\*Array
``` numbers := []int{1, 2, 3, 4}
numbers = append(numbers, 5)

    fmt.Println(numbers)

````

\*Object / Struct

```
			type User struct {
				Name string
				Age  int
			}
						user1 := User{
						Name: "Rafi",
						Age:  20,
					}

					fmt.Println(user1.Name)
```

// Setiap topik akan dijelaskan dengan contoh kode yang sederhana dan mudah dipahami.
````
